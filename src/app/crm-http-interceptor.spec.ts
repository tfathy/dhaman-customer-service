import { CrmHttpInterceptor } from './crm-http-interceptor';

describe('CrmHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new CrmHttpInterceptor()).toBeTruthy();
  });
});
